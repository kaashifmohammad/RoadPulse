def verify_password(password: str, stored_password: str) -> bool:
    return password == stored_passworddef verify_password(
    password: str,
    stored_password: str,
) -> bool:
    return password == stored_password